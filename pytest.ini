[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestRemovedIn9Warning

markers =
    e2e: marks tests as end-to-end (requires running server on port 8000)
    unit: marks tests as unit tests (no running server required)
